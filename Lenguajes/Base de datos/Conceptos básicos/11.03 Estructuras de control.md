- Solo se dispone de la estructura condicional IF.
<font style="color:salmon; font-weight:bold">Sintaxis:</font>
```sql
DECLARE
-- Sección para declaración de variables
BEGIN
IF (expresión) THEN
-- Instrucciones
ELSIF (expresión) THEN
-- Instrucciones
ELSE
-- Instrucciones
END IF;
END ;
```
