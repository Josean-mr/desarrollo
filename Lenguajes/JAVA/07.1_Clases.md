# ¿Qué son las clases?
<iframe width="560" height="315" src="https://www.youtube.com/embed/q9gZ9fjKIC0?si=jjwjJedWdgCPe1Br" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
- Es una **plantilla o molde que permite construir objetos**. Ejemplo: Si tuviéramos la clase Automóvil, esta sería el plano para construir un Objeto (un o varios automóviles).
- Representan "entidades" del mundo real.
- Poseen **atributos** y **métodos**. También puede poseer constructor.
- Las clases no se pueden utilizar directamente. Se han de utilizar instancias a objetos.
# ¿Para qué se usan las clases?
Principalmente para instanciar / crear objetos a partir de ellas.
## <font style = "color:FireBrick">Atributos</font>
### ¿Qué son?

Son variables que se declaran como una variable normal. Estas variables sirven como características de esta clase. Son las propiedades que tiene.
### Tipos
#### Según tipo de dato
- Datos primitivos:
	- int, double, char...
- Datos complejos:
	- Otros objetos que pertenecen a clases (como String).
#### Según tipo de variable
![[Clases-AtributosSinEjemplos.png]]
- Variable de **instancia**:
	- Son las variables normales.
		- Cada galleta tiene su propia copia.
	- Ejemplo:
		public class Persona {  
			private String nombre; // Atributo de instancia  
			private int edad; // Atributo de instancia
		}
- Variables de **clase** (Static):
	- Implican una sola zona de memoria reservada para las instancias de la clase.
		- Las 3 galletas comparten el valor de la variable.
		- Ejemplo:
			public class Contador {  
				public static int count; // Atributo estático  
			}
- Variables **finales o constantes** (final):
	- Tienen un valor que no cambia a lo largo del programa.
	- Ejemplo:
public class Constantes{  
	public final double PI = 3.14; // Atributo final de instancia  
	public static final int MAX_VALUE = 100; // Atributo final estático  
}

